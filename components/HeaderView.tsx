import { usePlayerContext } from 'lib/playerContext'

export const HeaderView = () => {
  // const playerContext = usePlayerContext()
  // const { activeSong } = playerContext ?? {}

  return <header>Header Active Song: {/*activeSong.title */}</header>
}
